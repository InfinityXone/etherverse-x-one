# Testing Procedures
1. Unit tests — basic logic validation.
2. Integration tests — check full workflow.
3. Regression tests — ensure no new errors introduced.
4. Sandbox verification — Helix tests code in isolation before applying fixes.
